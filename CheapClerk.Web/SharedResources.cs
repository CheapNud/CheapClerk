namespace CheapClerk.Web;

/// <summary>
/// Marker class for the shared localization resources (Resources/SharedResources.resx).
/// Carries no members; exists only so <c>IStringLocalizer&lt;SharedResources&gt;</c> can
/// resolve the resx files sitting next to it.
/// </summary>
public sealed class SharedResources;
